/**
 * Title of Project
 * Author Name
 * 
 * HOW EMBARRASSING! I HAVE NO DESCRIPTION OF MY PROJECT!
 * PLEASE REMOVE A GRADE FROM MY WORK IF IT'S GRADED!
 */

"use strict";

/**
 * OH LOOK I DIDN'T DESCRIBE SETUP!!
*/

//creating moon
let moon = {
    x: 500,
    y: 300,
    fill: (171, 169, 159),
};

let createOne = {
    x: 600,
    y: 230,
    fill: (176, 177, 181, 200),
};

let createTwo = {
    x: 347,
    y: 400,
    fill: (176, 177, 181, 200),
}

let createThree = {
    x: 430,
    y: 400,
    fill: (176, 177, 181, 200),
}

let createFour = {
    x: 500,
    y: 360,
    fill: (176, 177, 181, 200),
}

let createFive = {
    x: 500,
    y: 230,
    fill: (520, 150, 181, 200),
}

let createSix = {
    x: 660,
    y: 330,
    fill: (630, 360, 181, 200),
}

let transp1 = {
    x: 600,
    y: 430,
    fill: (0, 400, 0, 165),
};

let transp2 = {
    x: 500,
    y: 450,
    fill: (0, 400, 0, 165),
};

let transp3 = {
    x: 430,
    y: 300,
    fill: (0, 400, 0, 165),
};

let transp4 = {
    x: 530,
    y: 130,
    fill: (0, 400, 0, 165),
};

let transp5 = {
    x: 340,
    y: 220,
    fill: (0, 400, 0, 165),
};

let transp6 = {
    x: 430,
    y: 230,
    fill: (0, 400, 0, 165),
};




function setup() {
    createCanvas(1200, 600)
    frameRate(3)

}


/**
 * OOPS I DIDN'T DESCRIBE WHAT MY DRAW DOES!
*/
function draw() {
    background(9, 20, 48);


    //STARS hehe i did 5
    push()
    fill('white');
    noStroke();
    let x1 = random(0, width); // x is anywhere horizontally
    let y1 = random(0, height); // y is anywhere vertically
    ellipse(x1, y1, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x2 = random(0, width); // x is anywhere horizontally
    let y2 = random(0, height); // y is anywhere vertically
    ellipse(x2, y2, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x3 = random(0, width); // x is anywhere horizontally
    let y3 = random(0, height); // y is anywhere vertically
    ellipse(x3, y3, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x4 = random(0, width); // x is anywhere horizontally
    let y4 = random(0, height); // y is anywhere vertically
    ellipse(x4, y4, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x5 = random(0, width); // x is anywhere horizontally
    let y5 = random(0, height); // y is anywhere vertically
    ellipse(x5, y5, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x6 = random(0, width); // x is anywhere horizontally
    let y6 = random(0, height); // y is anywhere vertically
    ellipse(x6, y6, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x7 = random(0, width); // x is anywhere horizontally
    let y7 = random(0, height); // y is anywhere vertically
    ellipse(x7, y7, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x8 = random(0, width); // x is anywhere horizontally
    let y8 = random(0, height); // y is anywhere vertically
    ellipse(x8, y8, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x9 = random(0, width); // x is anywhere horizontally
    let y9 = random(0, height); // y is anywhere vertically
    ellipse(x9, y9, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x10 = random(0, width); // x is anywhere horizontally
    let y10 = random(0, height); // y is anywhere vertically
    ellipse(x10, y10, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x11 = random(0, width); // x is anywhere horizontally
    let y11 = random(0, height); // y is anywhere vertically
    ellipse(x11, y11, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x12 = random(0, width); // x is anywhere horizontally
    let y12 = random(0, height); // y is anywhere vertically
    ellipse(x12, y12, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x13 = random(0, width); // x is anywhere horizontally
    let y13 = random(0, height); // y is anywhere vertically
    ellipse(x13, y13, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x14 = random(0, width); // x is anywhere horizontally
    let y14 = random(0, height); // y is anywhere vertically
    ellipse(x14, y14, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x15 = random(0, width); // x is anywhere horizontally
    let y15 = random(0, height); // y is anywhere vertically
    ellipse(x15, y15, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x16 = random(0, width); // x is anywhere horizontally
    let y16 = random(0, height); // y is anywhere vertically
    ellipse(x16, y16, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x17 = random(0, width); // x is anywhere horizontally
    let y17 = random(0, height); // y is anywhere vertically
    ellipse(x17, y17, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x18 = random(0, width); // x is anywhere horizontally
    let y18 = random(0, height); // y is anywhere vertically
    ellipse(x18, y18, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x19 = random(0, width); // x is anywhere horizontally
    let y19 = random(0, height); // y is anywhere vertically
    ellipse(x19, y19, 5);
    pop();

    push()
    fill('white');
    noStroke();
    let x20 = random(0, width); // x is anywhere horizontally
    let y20 = random(0, height); // y is anywhere vertically
    ellipse(x20, y20, 5);
    pop();

    moon.x += 5.5;
    //the moon
    push()
    fill(moon.fill);
    stroke(115, 116, 120, 20)
    // strokeWeight(50)
    ellipse(moon.x, moon.y, 450, 445)
    pop()


    createOne.x += 5.5;
    //cratere1
    push()
    fill(createOne.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createOne.x, createOne.y, 130)
    pop()

    createTwo.x += 5.5;
    //cratere2
    push()
    fill(createTwo.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createTwo.x, createTwo.y, 50)
    pop()

    createThree.x += 5.5;
    //cratere3
    push()
    fill(createThree.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createThree.x, createThree.y, 90)
    pop()

    createFour.x += 5.5;
    //cratere4
    push()
    fill(createFour.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createFour.x, createFour.y, 30)
    pop()

    createFive.x += 5.5;
    //cratere5
    push()
    fill(createFive.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createFive.x, createFive.y, 35)
    pop()

    createSix.x += 5.5;
    //cratere6
    push()
    fill(createSix.fill);
    stroke(77, 79, 87, 128)
    strokeWeight(6)
    ellipse(createSix.x, createSix.y, 50)
    pop()

    transp1.x += 5.5;
    push()
    fill(transp1.fill);
    noStroke();
    ellipse(transp1.x, transp1.y, 60, 60);
    pop()

    transp2.x += 5.5;
    push()
    fill(transp2.fill);
    noStroke();
    ellipse(transp2.x, transp2.y, 60, 60);
    pop()

    transp3.x += 5.5;
    push()
    fill(transp3.fill);
    noStroke();
    ellipse(transp3.x, transp3.y, 60, 60);
    pop()

    transp4.x += 5.5;
    push()
    fill(transp4.fill);
    noStroke();
    ellipse(transp4.x, transp4.y, 60, 60);
    pop()

    transp5.x += 5.5;
    push()
    fill(transp5.fill);
    noStroke();
    ellipse(transp5.x, transp5.y, 60, 60);
    pop()

    transp6.x += 5.5;
    push()
    fill(transp6.fill);
    noStroke();
    ellipse(transp6.x, transp6.y, 60, 60);
    pop()




}
